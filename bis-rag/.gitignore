# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Environment — never commit real API keys
.env

# Vector DB — auto-rebuilt by inference.py on first run
data/chroma_db/
# NOTE: chunks.json, valid_codes.json, public_results.json are COMMITTED

# Judge output — generated by inference.py, not part of source
judge_results.json
sample_output.json

# Verification script — local dev tool, not needed by judges
verify_submission.sh

# Internal planning docs — not needed in submission
DEMO_SCRIPT.md
SLIDES_OUTLINE.md
# NOTE: presentation.pdf is COMMITTED — rulebook requires it
*.pptx
*.key

# Node
frontend/node_modules/
frontend/dist/
frontend/package-lock.json

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
